server.port: 8080

logging.level.root: INFO

mongo:
  host: localhost
  port: 27017
  db.name: test

spring.jackson:
  default-property-inclusion: non_null

management:
  endpoints:
    web:
      exposure:
        include: health,info